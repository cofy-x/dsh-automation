# Composable service bundle contributed to every consuming profile. Application
# command parsing lives in the separate dsh-automation-app bundle so Web and
# other hosts can consume the service without mounting a second app.
- insert:
    - id: dsh-automation
      name: dsh-automation
