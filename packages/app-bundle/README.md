# dsh-automation-app

Dedicated DSH application bundle for `dsh-automation` Worker and management profiles.

Install this companion together with `dsh-automation` only in a dedicated automation profile. Web and Trigger profiles consume the service package without this application bundle.
